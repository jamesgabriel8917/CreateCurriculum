# CreateCurriculum

A web-based curriculum generator that creates professional DOCX documents locally.

## 🚀 Quick Start

### Option 1: Using the Shell Script (Recommended for macOS/Linux)

```bash
./start.sh
```

The script will:
- Check if Node.js is installed
- Install dependencies automatically
- Start the local server

### Option 2: Manual Start

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Start the server:**
   ```bash
   npm start
   ```

3. **Open in browser:**
   Navigate to `http://localhost:3000`

## 📋 Features

- 🎨 Beautiful, responsive web interface
- 📄 Generate professional DOCX curriculum
- ⚡ Fast, offline-capable generation
- 🔒 All data processed locally
- 📥 Direct download functionality

## 🎯 How to Use

1. Run `./start.sh` or `npm start`
2. Open http://localhost:3000 in your browser
3. Click "Generate Curriculum"
4. Your DOCX file will download automatically